import React  from 'react';
import { Typography,Empty  } from 'antd';

const { Title } = Typography;

const Endpoint = () => (
    <div className="Endpoint">
        <Title level={2}>我懒，不想写页面了</Title>
        <Empty />
    </div >
);


export default Endpoint;
